import { FilterNodesPipe } from './filter-nodes.pipe';

describe('FilterNodesPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterNodesPipe();
        expect(pipe).toBeTruthy();
    });
});
