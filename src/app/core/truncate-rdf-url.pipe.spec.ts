import { TruncateRdfUrlPipe } from './truncate-rdf-url.pipe';

describe('TruncateRdfUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateRdfUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
