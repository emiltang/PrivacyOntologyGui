import {SortRiskPipe} from './sort-risk.pipe';

describe('SortRiskPipe', () => {
    it('create an instance', () => {
        const pipe = new SortRiskPipe();
        expect(pipe).toBeTruthy();
    });
});
